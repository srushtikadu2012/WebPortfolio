const typed = new Typed('#typed-text', {
  strings: ['Srushti_Kadu', 'Software Developer'],
  typeSpeed: 60,
  backSpeed: 30,
  loop: true
});

// AOS init
AOS.init({
  duration: 1000,
  once: true
});